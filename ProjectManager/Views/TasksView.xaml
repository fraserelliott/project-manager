<UserControl x:Class="ProjectManager.Views.TasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Button Content="New Task" Style="{StaticResource PrimaryButton}" Command="{Binding NewTaskCommand}" HorizontalAlignment="Left" />
        </Grid>

        <DataGrid Grid.Row="1"
          ItemsSource="{Binding Tasks}"
          SelectedItem="{Binding SelectedTask, Mode=TwoWay}"
          SelectionMode="Single"
          SelectionUnit="FullRow"
          EnableRowVirtualization="True"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          IsReadOnly="True"
          Background="{StaticResource BgPrimary}"
          Foreground="{StaticResource TextPrimary}"
          GridLinesVisibility="None"
          HeadersVisibility="Column"
          RowStyle="{StaticResource TaskDataGridRowStyle}" 
          >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" ElementStyle="{StaticResource GridTextStyle}"/>
                <DataGridTemplateColumn Header="Alerts" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <StackPanel.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="Margin" Value="0,0,8,0"/>
                                    </Style>
                                </StackPanel.Resources>

                                <!-- Blocked -->
                                <Border CornerRadius="6"
                                    Padding="8,3"
                                    Margin="0,0,8,0"
                                    Background="{StaticResource BtnDanger}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsBlocked}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <TextBlock Foreground="{StaticResource BgPrimary}"
                           FontFamily="{StaticResource FontPrimary}"
                           FontSize="12"
                           VerticalAlignment="Center">
                            <Run FontFamily="Segoe MDL2 Assets" Text="&#xE711;"/>
                            <Run Text="  Blocked"/>
                                    </TextBlock>
                                </Border>

                                <!-- Stale -->
                                <Border CornerRadius="6"
                        Padding="8,3"
                        Background="#F59E0B">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsStale}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <TextBlock Foreground="{StaticResource BgPrimary}"
                               FontFamily="{StaticResource FontPrimary}"
                               FontSize="12"
                               VerticalAlignment="Center">
                            <Run FontFamily="Segoe MDL2 Assets" Text="&#xE7BA;"/>
                            <Run Text="  Stale"/>
                                    </TextBlock>
                                </Border>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{Binding ButtonText}" Command="{Binding AdvanceStatusCommand}" CommandParameter="{Binding Id}" Style="{StaticResource PrimaryButtonCompact}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="*" ElementStyle="{StaticResource GridTextStyle}"/>
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Details" Command="{Binding DataContext.ShowDetailsCommand,RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding Id}" Style="{StaticResource PrimaryButtonCompact}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
    
</UserControl>
