<Window x:Class="ProjectManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProjectManager" Height="600" Width="1000">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding ProjectName}" FontSize="22" FontWeight="SemiBold"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Tasks}"
                  SelectedItem="{Binding SelectedTask, Mode=TwoWay}"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  EnableRowVirtualization="True"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Background="{StaticResource BgPrimary}"
                  Foreground="{StaticResource TextPrimary}"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  RowStyle="{StaticResource TaskDataGridRowStyle}" 
                  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" ElementStyle="{StaticResource GridTextStyle}"/>
                <DataGridTemplateColumn Header="Alerts" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <StackPanel.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="Margin" Value="0,0,8,0"/>
                                    </Style>
                                </StackPanel.Resources>

                                <!-- Blocked -->
                                <Border CornerRadius="6"
                                    Padding="8,3"
                                    Margin="0,0,8,0"
                                    Background="{StaticResource BtnDanger}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsBlocked}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <TextBlock Foreground="{StaticResource BgPrimary}"
                                       FontFamily="{StaticResource FontPrimary}"
                                       FontSize="12"
                                       VerticalAlignment="Center">
                                        <Run FontFamily="Segoe MDL2 Assets" Text="&#xE711;"/>
                                        <Run Text="  Blocked"/>
                                    </TextBlock>
                                </Border>

                                <!-- Stale -->
                                <Border CornerRadius="6"
                                    Padding="8,3"
                                    Background="#F59E0B">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsStale}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <TextBlock Foreground="{StaticResource BgPrimary}"
                                           FontFamily="{StaticResource FontPrimary}"
                                           FontSize="12"
                                           VerticalAlignment="Center">
                                        <Run FontFamily="Segoe MDL2 Assets" Text="&#xE7BA;"/>
                                        <Run Text="  Stale"/>
                                    </TextBlock>
                                </Border>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{Binding ButtonText}"
                            Command="{Binding DataContext.AdvanceStatusCommand,RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding Id}"
                            Style="{StaticResource PrimaryButtonCompactStyle}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="*" ElementStyle="{StaticResource GridTextStyle}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>