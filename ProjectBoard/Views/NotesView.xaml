<UserControl x:Class="ProjectBoard.Views.NotesView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:ProjectBoard.Controls" mc:Ignorable="d" d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Button Content="New Note" Style="{StaticResource PrimaryButton}" Margin="0,0,0,8"
                        Command="{Binding NewNoteCommand}" />
            </StackPanel>

            <TextBlock Grid.Row="1" Text="Search" Style="{StaticResource VerticalFieldLabel}" />

            <Grid Grid.Row="2" Margin="0,0,0,8">

                <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center" Style="{StaticResource FieldTextBox}" Padding="6,2,22,2" />
                <Button Content="&#xE711;" Style="{StaticResource PrimaryIconButton}" Margin="0,0,4,0"
                        Command="{Binding ClearSearchCommand}" Width="16" Height="16" MinHeight="16" FontSize="10"
                        HorizontalAlignment="Right"
                        Visibility="{Binding SearchText, Converter={StaticResource StringNotEmptyToVisibilityConverter}}" />
            </Grid>

            <ListView Grid.Row="3" ItemsSource="{Binding NotesView}" SelectedItem="{Binding SelectedNote, Mode=TwoWay}" />
        </Grid>

        <Grid Grid.Column="1" Margin="8,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0, 0, 0, 8"
                        Visibility="{Binding HasSelectedNote, Converter={StaticResource BoolToVisibilityConverter}}">
                <ToggleButton IsChecked="{Binding IsEditing, Mode=TwoWay}" Style="{StaticResource EditToggleButton}" />
                <Button Content="Close" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"
                        Command="{Binding CloseNoteCommand}" />
                <Button Content="Delete" Style="{StaticResource DangerButton}" Margin="8,0,0,0"
                        Command="{Binding DeleteNoteCommand}" CommandParameter="{Binding SelectedNote.Id}" />
            </StackPanel>

            <controls:MarkdownView Grid.Row="1" Text="{Binding SelectedNote.Text, Mode=TwoWay}"
                                   ViewMode="{Binding MarkdownViewMode}"
                                   Visibility="{Binding HasSelectedNote, Converter={StaticResource BoolToVisibilityConverter}}" />

            <controls:MarkdownView Grid.Row="1" Text="{Binding UnselectedText}"
                                   Visibility="{Binding HasSelectedNote, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
        </Grid>
    </Grid>
</UserControl>